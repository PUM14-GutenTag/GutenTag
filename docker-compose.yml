# Main Compose file.
version: "3.8"

services:
  db:
    image: postgres:13.2-alpine

  backend:
    build:
      context: ./backend
    depends_on:
      - db
    ports:
      - 5000:5000

  frontend:
    build:
      context: ./frontend
    ports:
      - 3000:3000
