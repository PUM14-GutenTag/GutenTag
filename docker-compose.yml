# Main Compose file.
version: "3.8"

services:
  db:
    image: postgres:13.2-alpine

  backend:
    build: ./backend
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      args:
        REACT_APP_SERVER_NAME: ${SERVER_NAME}
